input UserInput {
    name: String!
    username: String!
    password: String!
}

type User {
    id: Int
    name: String
}

type AuthenticatedUser {
    token: String
    user: User
}

type Mutation {
    createUser(data: UserInput!): AuthenticatedUser!
}