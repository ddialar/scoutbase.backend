# import ApiError from '../ApiError/ApiError.graphql'
# import Actor from '../actor/actor.graphql'

type Movie {
    title: String
    year: Int
    rating: Float
    actors: [Actor]
}

type Query {
    movies: [Movie]
}